# <folder>/

<One line: what this folder is responsible for.>

## Files

| File | Purpose |
|------|---------|
| `<file>` | <one line> |

## Subdirectories

<Only if this folder has subdirectories. One line each — each has its own
CLAUDE.md carrying the detail.>

| Directory | Purpose |
|-----------|---------|
| `<subdir>/` | <one line> |

## Conventions

- <Local rules that apply to code in this folder.>

## Notes

- <Gotchas, non-obvious history, things a future reader would otherwise trip on.>

<!-- Subfolder CLAUDE.md. Cap = C / 2^depth (depth 1 → C/2, depth 2 → C/4, …).
Describe ONLY this directory. Push project-wide context up to the root; push
finer detail down into subdirectory maps. -->
